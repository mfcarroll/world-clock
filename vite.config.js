import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  // Vite will automatically detect and use the postcss.config.js 
  // and tailwind.config.js files in your project root.
});

